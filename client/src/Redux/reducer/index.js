import { } from "../actions"

const initialState={}


const rootReducer = (state = initialState, action) => {
// switch (key) {
//     case value:
        
//         break;

//     default:
//         initialState;
// }
}
export default rootReducer;
